---
type: Known Issue
title: WebUI dashboard and WebSocket broadcast not functioning
description: Dashboard fields show "--" for all values, WebSocket log push does not reach frontend, ESP_LOG forwarding to UART is unstable after serial driver reinit
tags: [webui, websocket, broadcast, logging, serial]
timestamp: 2026-07-10
status: active
---

# WebUI Dashboard and WebSocket Broadcast Issues

## 1. WebSocket frames do not reach frontend

### Problem
Dashboard fields (temperature, ADC, valve, burette) show `"--"` indefinitely. The WebSocket connection is established (`ws.readyState === 1`, `connectionAlive === true`), but `onmessage` never fires for broadcast data or log events.

### Root cause (hypothesis)
`broadcastWsEvent()` iterates `sessions_[]` array and calls `httpd_ws_send_frame_async()` for each valid session. The session is added in `ws_handler()` via `httpd_req_to_sockfd()` → `addSession(fd)`. Possible causes:
- `httpd_ws_get_fd_info()` returns non-WebSocket type for the stored fd
- `httpd_ws_send_frame_async()` returns an error, causing session invalidation
- `handle_` is null (server not running) — unlikely since curl works
- Frame payload (`const char* jsonData`) becomes stale if async send references it later — but ESP-IDF copies frame data internally

### Solution
Not yet determined. Diagnostics added via `write(STDOUT_FILENO, ...)` in `broadcastWsEvent()` are not visible in serial log (see Issue 2), making remote debugging difficult. Possible next steps:

1. Test with `httpd_ws_send_frame()` (synchronous) instead of `_async`
2. Log `esp_err_t` return value via `ESP_LOGI` (risk of recursion, but guarded by `pushing_`)
3. Test with a dedicated WS echo handler

### Edge cases
- Session fd may become stale if client reconnects (e.g., after flash). `broadcastWsEvent` cleans stale sessions by checking `httpd_ws_get_fd_info()`.
- Multiple WS clients (max 4 sessions). Not tested.

---

## 2. ESP_LOG forwarding to UART unstable after serial driver reinit

### Problem
After `esp_log_set_vprintf(logVprintf)` is called at the start of `app_main()`, subsequent `write(STDOUT_FILENO, buf, n)` calls in `logVprintf` do not produce visible output in the serial monitor. `puts("DBG: ...")` lines still appear. Broadcast JSON from `serial.write()` also works. Only the forwarded ESP_LOG output is lost.

### Root cause
The `SerialReader::init()` (step 4) calls `uart_driver_install()` for the console UART. This reinitializes the UART driver, which may change the internal file descriptor mapping for `stdout`/`STDOUT_FILENO`. After reinit, `write(1, ...)` may go to a different fd than `puts()`/`fwrite()`.

### Solution (attempted)
- `write(STDOUT_FILENO, buf, n)` — not visible
- `fwrite(buf, 1, n, stdout)` + `fflush(stdout)` — not yet verified

### Edge cases
- Early ESP_LOG calls (before step 4 serial init) may use different UART state than later calls
- `esp_log_set_vprintf` affects ALL ESP_LOG calls — any re-entrancy (ESP_LOG called from within vprintf) is silently dropped by `pushing_` guard

---

## 3. `/api/logs` response buffer size marginal

### Problem
`ResponseBuffer` (2048 bytes) holds approximately 20 log entries. With longer messages (e.g., JSON payloads, stack traces), fewer entries fit. The response is silently truncated when the buffer overflows, producing invalid JSON.

### Root cause
The `append()` lambda in `api_logs_handler` stops writing when `pos >= rsp.size()`. The JSON closing braces are then written at the truncated position, resulting in unterminated strings.

### Solution
- Increased from 512 to 2048 bytes (covers 20 typical entries)
- Future: implement chunked response or dynamic buffer for large queries

---

## 4. Frontend render race with WebSocket connection

### Problem
`loadInitialLogs()` (HTTP fetch of `/api/logs`) and `connectWs()` (WebSocket) are both called from `initApp` on `DOMContentLoaded`. If `loadInitialLogs` completes before WS is ready, initial log entries render but subsequent WS-pushed log events may be missed during the reconnect gap.

### Root cause
No synchronization between HTTP fetch and WS connection. The frontend uses `addLogEntry(msg)` for both paths independently, but the WS might not yet be connected when the initial fetch completes.

### Solution
Not critical — messages are pushed to the same `APP_STATE.logs.messages` array regardless of source. The gap only affects the first ~1 second of operation.

---

## 5. Broadcast JSON format mismatch with legacy spec

See `docs/plans/pending/26_07_10_alignment_with_legacy.md` Phase 8.

| Field | Legacy Spec | Current |
|-------|-------------|---------|
| `ts` (timestamp) | `"ts"` | `"t"` |
| Motor config `dir` | Not in broadcast | `"dir":"liq_in"` |
| Motor config `spd` (Hz) | Not in broadcast | `"spd":1000` |
| Motor config `acc` | Not in broadcast | `"acc":500` |
| Motor config `vol` | Not in broadcast | `"vol":50.0` |
| Motor config `steps` | Not in broadcast | `"steps":12345` |

---

## 6. Missing endpoints vs legacy

| Endpoint | Status |
|----------|--------|
| `GET /api/nvs/status` | Not implemented (Phase 7) |
| `GET /api/valve/set` (POST) | Combined into `POST /api/valve` |
| `GET /api/valve/state` | Combined into `GET /api/valve` |
| `GET /api/events` (SSE) | Replaced by `GET /ws/stream` (WebSocket) |

---

## 7. CDN dependency for dashboard

### Problem
`index.html` loads Bootstrap 5.3 CSS/JS from `cdn.jsdelivr.net`. In AP-only mode (no internet), the dashboard renders unstyled. The captive portal page (`/wifi`) is self-contained and works offline.

### Root cause
Inline CSS/JS would increase firmware size. CDN was chosen for convenience during development.

### Solution
Either bundle Bootstrap into the firmware SPIFFS/LittleFS, or use inline minimal CSS for the dashboard.

---

## 8. Visual appearance does not match legacy Arduino dashboard

### Problem
The current C++ inline WebUI (`components/interface/include/interface/webui.hpp`) has only 3 working accordion sections, all heavily simplified. The legacy Arduino (`legacy/arduino/data/index.html`) has 9 full sections with working controls.

### Section-by-section comparison

| # | Section | Legacy Arduino | Current (webui.hpp) | Gap |
|---|---------|---------------|---------------------|-----|
| 1 | Peripheral Status | 7 fields: temp, ADC, electrode, valve, 2 limit switches, connection | 4 fields: temp, ADC, valve, burette | Missing: electrode, limit switches, connection |
| 2 | Stepper Driver (UART) | StallGuard result, threshold (editable), overheat warning, motor busy | Not present | Entire section missing |
| 3 | Stepper Motor Control | Steps/continuous mode, Start/Stop button, direction (LIQ_IN/LIQ_OUT), EN pin status, steps counter, spinner | `initStepperControls()` is empty stub | No controls at all |
| 4 | System Log | Level filter dropdown (ALL/DEBUG/INFO/WARN/ERROR), Download button, Clear, textarea rows=8 | Clear button, textarea rows=6, no level filter, no download | Missing level filter, download |
| 5 | SSE/WS Events | Raw JSON checkbox, auto-update checkbox, event log with entries | Not present (WS `event === 'log'` handler added, but no WS events UI) | Entire section missing |
| 6 | ADC Calibration | 5-point table with #/ref mV/signal mV/action columns, a/b/R² display, calibrated date, Compute & Save button, Reset button | `loadCalibrationStatus()`, `loadBuretteCalStatus()`, `initSpeedTable()` are empty stubs | Full calibration UI |
| 7 | Pinout Table | 10-row GPIO-to-device mapping table with notes | Not present | Entire section missing |
| 8 | Burette Volume Calibration | Z-factor table, freq/mass/temp/pressure inputs, Run/Calculate/Reset/Save buttons, steps results | Stub functions only | Full calibration UI |
| 9 | Burette Speed Calibration | 3-point measurement table, Run/Calculate/Reset/Save buttons, k/R² results | Stub functions only | Full calibration UI |

### JavaScript files comparison

| File | Legacy (arduino/data/js/) | Current (webui.hpp inline) |
|------|--------------------------|----------------------------|
| `state.js` | 44 lines — CONFIG + APP_STATE | 9 lines (same structure, minimal) |
| `ui-update.js` | 170 lines — full DOM update for all 9 sections | 17 lines — only temp/ADC/valve/burette |
| `sse.js` / `ws.js` | 230 lines — full SSE client: reconnect, event dispatch, status/debug/log/limitsw events | 34 lines — WS connect + `updateUI()` dispatch |
| `logs.js` | 69 lines — level filter, download, formatting | 1 line: `window.setLogLevelFilter=function(l){}` |
| `stepper.js` | 75 lines — steps/continuous mode, start/stop, direction buttons, EN pin display | 1 line: `window.initStepperControls=function(){}` |
| `calibration.js` | 433 lines — ADC + burette volume + burette speed calibration UIs | 4 lines: 3 empty stubs |
| `init.js` | 87 lines — app init, sendCommand, toggleValve, sendCmd, loadInitialLogs, initApp | 44 lines — same structure, but bare minimum |

### Additional UI differences

| Feature | Legacy | Current |
|---------|--------|---------|
| Theme toggle | Moon icon toggle button in top-left | Not present |
| Title | "Panel otladki ESP32 EKOTITR" (Russian) | "EcoTiter Burette" (English) |
| Dashboard HTML lines | 663 (separate file) | 73 (inline in C++, mixed) |
| Captive portal | 143 lines, spinner, toggle password, hover effects | Similar but simplified |

### Root cause
The WebUI was rebuilt from scratch during the Arduino → Rust → C++ conversions, with only a minimal placeholder UI. The inline C++ `string_view` approach makes maintaining a full-featured dashboard impractical.

### Solution
1. Extract HTML/JS/CSS from `webui.hpp` into separate files.
2. Use `legacy/arduino/data/` as the authoritative source for HTML structure, JS logic, and CSS styling.
3. Adapt SSE event dispatch to WebSocket (`sse.js` → `ws.js`).
4. Implement all empty JS stubs:
   - P0: Calibration UI (`calibration.js`) — 433 lines, matches Phase 5 backend
   - P1: Stepper controls (`stepper.js`) — 75 lines
   - P2: Logs UI (`logs.js`) — level filter, download
   - P3: SSE/WS Events section
   - P4: Pinout Table
5. Add missing peripheral fields: electrode, limit switches, connection.

### See also
- Legacy HTML: `legacy/arduino/data/index.html` (663 lines)
- Legacy captive: `legacy/arduino/data/captive.html` (143 lines)
- Legacy CSS: `legacy/arduino/data/css/`
- Legacy JS: `legacy/arduino/data/js/` (7 files, ~850 lines total)
- Current inline: `components/interface/include/interface/webui.hpp` (319 lines)
- Plan: `docs/plans/pending/26_07_10_alignment_with_legacy.md`
